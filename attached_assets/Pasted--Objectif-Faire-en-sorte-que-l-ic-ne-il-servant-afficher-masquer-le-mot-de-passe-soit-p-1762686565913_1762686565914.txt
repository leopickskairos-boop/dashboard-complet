# 🎯 Objectif
Faire en sorte que l’icône “œil” servant à afficher / masquer le mot de passe soit **placée à la fin du champ**, à l’intérieur de la barre de saisie.

---

## 🧱 Étapes à appliquer

1️⃣ Ouvre ton composant d’inscription (`SignupForm.jsx`, `Register.jsx` ou équivalent).

2️⃣ Repère le champ mot de passe :
```jsx
<input
  type={showPassword ? "text" : "password"}
  placeholder="Mot de passe"
/>
```

3️⃣ Remplace ce bloc complet par la version suivante :

---

## ✅ Code complet (React + Tailwind + Lucide)

```jsx
import { useState } from "react";
import { Eye, EyeOff } from "lucide-react";

export default function PasswordField({ label, value, onChange }) {
  const [showPassword, setShowPassword] = useState(false);

  return (
    <div className="mb-4">
      {label && (
        <label className="block text-sm font-medium mb-1">{label}</label>
      )}

      <div className="relative">
        <input
          type={showPassword ? "text" : "password"}
          value={value}
          onChange={onChange}
          placeholder="••••••••"
          className="w-full border border-gray-300 rounded-lg py-3 pl-3 pr-10 focus:outline-none focus:ring-2 focus:ring-blue-500"
          required
        />
        <button
          type="button"
          onClick={() => setShowPassword(!showPassword)}
          className="absolute right-3 top-1/2 transform -translate-y-1/2 text-gray-500 hover:text-gray-700"
        >
          {showPassword ? <EyeOff size={20} /> : <Eye size={20} />}
        </button>
      </div>
    </div>
  );
}
```

---

## 📦 Si besoin, installe les icônes Lucide :
Dans le shell Replit :
```
npm install lucide-react
```

---

## 🧩 Exemple d’utilisation dans ton formulaire :

```jsx
<PasswordField
  label="Mot de passe"
  value={password}
  onChange={(e) => setPassword(e.target.value)}
/>

<PasswordField
  label="Confirme ton mot de passe"
  value={confirmPassword}
  onChange={(e) => setConfirmPassword(e.target.value)}
/>
```

---

## ✅ Résultat attendu

| Élément | Résultat |
|----------|-----------|
| Icône 👁️ | Positionnée **à droite du champ**, **dans la même barre** |
| Alignement vertical | Parfaitement centré |
| Design | Moderne, minimaliste et clair |
| Accessibilité | Le clic sur l’œil affiche / masque le mot de passe |

---

### 🧠 Résumé rapide
| Étape | Action |
|--------|---------|
| 1️⃣ | Remplacer ton champ input par le code ci-dessus |
| 2️⃣ | Ajouter le bouton œil à l’intérieur du champ |
| 3️⃣ | Tester le rendu → l’œil apparaît bien **à la fin du champ** |
| 4️⃣ | Cliquer dessus alterne texte / mot de passe |

